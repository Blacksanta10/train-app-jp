/**
 * This contains diffrent logos :))
 */


// Train emoji haha
export function Logo({ className = "" }: { className?: string }) {
  return (
    <div
      className={`flex items-center justify-center font-bold text-xl ${className}`}>
      🚄
    </div>
  );
}